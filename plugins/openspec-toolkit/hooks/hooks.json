{
  "description": "OpenSpec workflow guardrails: pre-apply checks, pre-archive review gate, and session context injection",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash|Write|Edit",
        "hooks": [
          {
            "type": "prompt",
            "prompt": "Before allowing this tool call during an /opsx:apply workflow, check:\n\n1. Missing config.yaml: If the user is working on an OpenSpec change (there's an openspec/changes/ directory with an active change), check if openspec/config.yaml exists. If it does NOT exist, output this warning:\n> Warning: No openspec/config.yaml found. Consider loading the pre-propose skill first to map the codebase and generate a config seed. This ensures your implementation respects existing conventions.\nThen allow the tool call to proceed (warn, don't block).\n\n2. Vague acceptance criteria: If a tasks.md file exists for the current change, check whether it has measurable acceptance criteria. Look for Given/When/Then patterns, specific expected values or behaviors, testable conditions (returns X, creates Y, status code Z). If tasks.md has tasks with NO measurable criteria (only vague descriptions like 'implement feature' or 'add support for X'), output this warning:\n> Warning: Some tasks in tasks.md have vague acceptance criteria. Consider adding Given/When/Then conditions or specific expected outcomes. This makes TDD and spec-review more effective.\nThen allow the tool call to proceed.\n\nOnly check these conditions when the conversation context indicates an /opsx:apply workflow is active. Do not warn during unrelated tool calls.",
            "timeout": 30
          }
        ]
      },
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "prompt",
            "prompt": "Before allowing a Bash tool call that runs /opsx:archive or opsx:bulk-archive, check if a quality review was performed in the current session.\n\nLook through the conversation history for evidence that one of these was run:\n- The post-apply skill was loaded and produced a Go/No-Go report\n- The spec-review skill was loaded and produced a review report\n- /opsx:verify was run\n\nIf NONE of these reviews were detected in the current conversation, output this warning:\n> Warning: No quality review detected in this session. Consider loading the post-apply skill (full quality gate) or spec-review skill (lighter review) before archiving. This catches spec gaps, security issues, and code quality problems.\nThen allow the archive command to proceed (warn, don't block).\n\nIf a review WAS detected, allow the command silently.",
            "timeout": 30
          }
        ]
      }
    ],
    "SessionStart": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "bash ${CLAUDE_PLUGIN_ROOT}/hooks/session-context.sh",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
